{
  "enabled": true,
  "name": "자동 테스트 실행",
  "description": "JavaScript 또는 Python 파일이 편집될 때마다 자동으로 관련 테스트를 실행합니다",
  "version": "1",
  "when": {
    "type": "fileEdited",
    "patterns": [
      "**/*.js",
      "**/*.jsx",
      "**/*.ts",
      "**/*.tsx",
      "**/*.py"
    ]
  },
  "then": {
    "type": "runCommand",
    "command": "if [[ \"${FILE_PATH}\" == *.py ]]; then pytest tests/ -v; elif [[ \"${FILE_PATH}\" == *.js ]] || [[ \"${FILE_PATH}\" == *.jsx ]] || [[ \"${FILE_PATH}\" == *.ts ]] || [[ \"${FILE_PATH}\" == *.tsx ]]; then cd frontend && npm test; fi"
  }
}